name: 🚀 Deploy to VPS

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout repo
        uses: actions/checkout@v3

      - name: 🚀 Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: root
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            APP_DIR="/root/musicBot"  # &lt;--- ИСПРАВЛЕНИЕ #1
            
            echo "📁 Создание директории приложения..."
            mkdir -p "$APP_DIR"
            
            cd "$APP_DIR"

            echo "🔄 Обновление кода из репозитория..."
            git config --global --add safe.directory "$APP_DIR"
            git fetch origin main
            git reset --hard origin/main
            git clean -fd

            echo "🤫 Создание .env файла из секретов..."
            echo "BOT_TOKEN=${{ secrets.BOT_TOKEN }}" > .env
            echo "QOBUZ_LOGIN=${{ secrets.QOBUZ_LOGIN }}" >> .env
            echo "QOBUZ_PASSWORD=${{ secrets.QOBUZ_PASSWORD }}" >> .env
            echo "AUDD_API_TOKEN=${{ secrets.AUDD_API_TOKEN }}" >> .env
            echo "SPOTIPY_CLIENT_ID=${{ secrets.SPOTIPY_CLIENT_ID }}" >> .env
            echo "SPOTIPY_CLIENT_SECRET=${{ secrets.SPOTIPY_CLIENT_SECRET }}" >> .env
            echo "✅ .env файл создан."

            # --- ИСПРАВЛЕНИЕ QOBUZ (ПРАВИЛЬНЫЙ ПУТЬ ДЛЯ ROOT) ---
            echo "⚙️ Создание конфигурации Qobuz..."
            
            # 1. Убираем защиту со старых неправильных файлов
            chattr -i "/opt/musicBot/home/.config/qobuz-dl/config.ini" || true
            chattr -i "/opt/musicBot/home/.config/qobuz-dl" || true
            chattr -i "/home/musicbot/.config/qobuz-dl/config.ini" || true
            chattr -i "/home/musicbot/.config/qobuz-dl" || true

            # 2. Указываем на настоящий домашний каталог пользователя ROOT
            QOBUZ_CONFIG_DIR="/root/.config/qobuz-dl" # &lt;--- ИСПРАВЛЕНИЕ #2
            QOBUZ_CONFIG_FILE="$QOBUZ_CONFIG_DIR/config.ini"
            
            mkdir -p "$QOBUZ_CONFIG_DIR"
            
            cat > "$QOBUZ_CONFIG_FILE" <<- EOF
            [DEFAULT]
            email = ${{ secrets.QOBUZ_LOGIN }}
            password = ${{ secrets.QOBUZ_PASSWORD }}
            default_folder =
            default_quality = 27
            default_limit = none
            no_m3u = true
            EOF
            
            echo "✅ config.ini для Qobuz создан в /root/.config/."
            # --- КОНЕЦ ИСПРАВЛЕНИЯ QOBUZ ---

            echo "🐍 Настройка виртуального окружения..."
            if [ ! -d "venv" ]; then
              python3.11 -m venv venv
            fi
            source venv/bin/activate

            echo "📦 Установка зависимостей Python..."
            pip install --upgrade pip
            pip install -r requirements.txt
            
            echo "🔧 Установка YouTube загрузчиков..."
            pip uninstall -y youtube-dl yt-dlp yt-dlp-compat-youtube-dl
            pip install -U youtube-dl
            pip install -U yt-dlp

            echo "📁 Создание необходимых директорий..."
            mkdir -p logs
            mkdir -p Qobuz/Downloads
            
            # Команда chown -R musicbot:musicbot "$APP_DIR" УДАЛЕНА, 
            # так как сервис работает от root и папка /root/musicBot 
            # и так принадлежит root.

            echo "♻️ Перезапуск сервиса musicbot..."
            systemctl daemon-reload
            systemctl restart musicbot.service

            echo "✅ Развертывание успешно завершено!"